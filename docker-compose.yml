version: '3.8'

services:

  mysql:
    image: mysql:8.0.33
    ports:
      - 3306:3306
    container_name: mysql
    environment:
      MYSQL_USER: gvendas
      MYSQL_PASSWORD: gvendas
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: gvendas_db